name: Reusable SYCL Windows build workflow

on:
  workflow_call:
    inputs:
      cxx:
        type: string
        required: false
        default: "cl"
      build_cache_suffix:
        type: string
        required: false
        default: "default"
      ref:
        type: string
        required: False
      build_configure_extra_args:
        type: string
        required: false
      build_target:
        type: string
        required: false
        default: sycl-toolchain

      changes:
        type: string
        description: 'Filter matches for the changed files in the PR'
        default: '[llvm, clang, sycl, llvm_spirv, xptifw, libclc]'
        required: false

      # Artifacts:
      retention-days:
        description: 'Artifacts retention period'
        type: string
        default: 3

      toolchain_artifact_filename:
        type: string
        default: llvm_sycl.tar.gz

      release_toolchain_artifact:
        type: string
        required: false
        description: |
          If provided, create an additional toolchain artifact without utilities
          used for testing in it. File name of the archive is the same as in the
          `toolchain_artifact` above.

      e2e_binaries_artifact:
        type: string
        required: false

      e2e_binaries_new_offload_model_artifact:
        type: string
        required: false

    outputs:
      build_conclusion:
        value: ${{ jobs.build.outputs.build_conclusion }}
      toolchain_artifact_filename:
        value: ${{ inputs.toolchain_artifact_filename }}

  workflow_dispatch:
    inputs:
      changes:
        description: 'Filter matches for the changed files in the PR'
        type: choice
        options:
          - "[]"
          - '[sycl]'
          - '[llvm, clang, sycl, llvm_spirv, xptifw, libclc]'
      build_cache_suffix:
        type: choice
        options:
          - "default"
      build_configure_extra_args:
        type: string
        required: false
      build_target:
        type: choice
        options:
          - "sycl-toolchain"
          - "all"
      toolchain_artifact_filename:
        type: choice
        options:
          - 'llvm_sycl.tar.gz'
        default:
      retention-days:
        type: choice
        options:
          - 3
      cxx:
        type: choice
        options:
          - cl
          - icx

permissions: read-all

jobs:
  build:
    name: Build + LIT
    runs-on: [Windows, build]
    outputs:
      build_conclusion: ${{ steps.build.conclusion }}
    steps:
    - name:  Detect hung tests
      if: always()
      shell: powershell
      run: |
        Invoke-WebRequest -Uri "https://raw.githubusercontent.com/intel/llvm/refs/heads/sycl/devops/scripts/windows_detect_hung_tests.ps1" -OutFile "windows_detect_hung_tests.ps1" -Headers @{Authorization = "Bearer ${{ github.token }}"}
        powershell.exe -File windows_detect_hung_tests.ps1
        $exitCode = $LASTEXITCODE
        Remove-Item -Path "windows_detect_hung_tests.ps1"
        exit $exitCode
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      with:
        sparse-checkout: |
          devops/actions
    - uses: ilammy/msvc-dev-cmd@0b201ec74fa43914dc39ae48a89fd1d8cb592756 # v1.13.0
      with:
        arch: amd64
    - name: Setup oneAPI env
      uses: ./devops/actions/setup_windows_oneapi_env
      if: ${{ !cancelled() && inputs.cxx == 'icx' }}
    - name: Set env
      run: |
        git config --system core.longpaths true
        git config --global core.autocrlf false
        echo "C:\Program Files\Git\usr\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
        echo "CCACHE_DIR=D:\github\_work\cache\${{ inputs.build_cache_suffix }}" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
        echo "CCACHE_MAXSIZE=10G" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
        echo "LIT_OPTS='-j$env:NUMBER_OF_PROCESSORS $LIT_OPTS'" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
    - name: Register cleanup after job is finished
      uses: ./devops/actions/cleanup
    - uses: ./devops/actions/cached_checkout
      with:
        path: src
        ref: ${{ inputs.ref || github.sha }}
        cache_path: "D:\\\\github\\\\_work\\\\repo_cache\\\\"
    - name: Configure
      shell: cmd
      env:
         ARGS: ${{ inputs.build_configure_extra_args }}
      # TODO switch to clang-cl and lld when this is fixed https://github.com/oneapi-src/level-zero/issues/83
      run: |
        mkdir build
        mkdir install
        IF NOT EXIST D:\github\_work\cache MKDIR D:\github\_work\cache
        IF NOT EXIST D:\github\_work\cache\${{inputs.build_cache_suffix}} MKDIR D:\github\_work\cache\${{inputs.build_cache_suffix}}
        python.exe src/buildbot/configure.py -o build ^
          --ci-defaults --use-zstd %ARGS% ^
          "-DCMAKE_C_COMPILER=${{inputs.cxx}}" ^
          "-DCMAKE_CXX_COMPILER=${{inputs.cxx}}" ^
          "-DCMAKE_INSTALL_PREFIX=%GITHUB_WORKSPACE%\install" ^
          -DCMAKE_CXX_COMPILER_LAUNCHER=ccache ^
          -DCMAKE_C_COMPILER_LAUNCHER=ccache ^
          -DLLVM_INSTALL_UTILS=ON
    - name: Build
      id: build
      shell: bash
      run: |
        cmake --build build --target ${{ inputs.build_target }}
    - run: build/bin/clang++ --version
    - name: check-llvm
      if: ${{ !cancelled() && contains(inputs.changes, 'llvm') }}
      shell: bash
      run: |      
        cmake --build build --target check-llvm
    - name: check-clang
      if: ${{ !cancelled() && contains(inputs.changes, 'clang') }}
      run: |
        cmake --build build --target check-clang
    - name: check-sycl
      if: ${{ !cancelled() && contains(inputs.changes, 'sycl') }}
      shell: bash
      run: |        
         cmake --build build --target check-sycl
    - name: check-sycl-unittests
      if: ${{ !cancelled() && contains(inputs.changes, 'sycl') }}
      # DIAGNOSTIC RUN for intel/llvm#22367 (NOT for merge).
      # Goal: capture the exact stack of the use-after-free crash that happens
      # *during* SubDevices.BuildProgramForSubSubDevices (a Managed<ur_program>
      # releasing its UR program through an already-freed adapter_impl). gtest
      # normally swallows the SEH ("thrown in the test body") and prints no
      # stack, so we run the single crashing test under the Windows debugger
      # (cdb) with --gtest_catch_exceptions=0 and break on the access violation.
      # Even when the raw-pointer build jumps through a nulled func-ptr table
      # (RIP=0), the return address left on the stack names the call_nocheck /
      # ~Managed caller. cdb ships with the Windows SDK "Debuggers" component;
      # the step is best-effort and tolerates the crash so the upload still runs.
      # Build only the program_manager binary (the full check-sycl-unittests
      # target builds+runs every unittest exe and was timing out).
      # Two independent capture paths so we are NOT dependent on cdb being on the
      # runner:
      #   (1) If cdb.exe (Windows SDK "Debuggers" component) is present, run the
      #       crashing test under it and print the fault stack inline.
      #   (2) ALWAYS also arm Windows Error Reporting LocalDumps (registry-only,
      #       built into Windows -- no tool needed) so the OS writes a full .dmp
      #       on the crash regardless of cdb. We upload the .dmp and symbolize it
      #       offline (pip install minidump + llvm-symbolizer --relative-address,
      #       the recipe already used for this bug). So if cdb is missing we still
      #       get a dump to analyze, not an empty log.
      # The crash site is already known (~Managed<ur_program> releasing through an
      # adapter whose func-ptr table is null). This run answers WHICH adapter:
      # SYCL_TRACE_ADAPTER_UAF makes adapter_impl ctor/dtor register/unregister in
      # a live set and ~Managed emit, before releasing, whether its adapter
      # pointer is still live. LIVE=0 STALE_OR_FREED => use-after-free / two-adapter
      # lifetime bug; LIVE=1 with a null firstFuncPtr => a live but reset/unpopulated
      # adapter. My getenv takes the path literally (no UR ':' parsing issue), so a
      # Windows path is fine here. set +e: the test still crashes (SEH), tolerate it.
      shell: bash
      env:
        SYCL_TRACE_ADAPTER_UAF: ${{ github.workspace }}\build\adapter_uaf_diag.log
      run: |
        set +e +o pipefail
        cmake --build build --target ProgramManagerTests-Non_Preview_Tests
        EXE="$(pwd)/build/tools/sycl/unittests/program_manager/ProgramManagerTests-Non_Preview_Tests.exe"
        export PATH="$(pwd)/build/bin:$PATH"
        export SYCL_CONFIG_FILE_NAME=null.cfg
        export SYCL_DEVICELIB_NO_FALLBACK=1
        export SYCL_CACHE_DIR="$(pwd)/build/sycl_cache"
        # --gtest_catch_exceptions=0 lets the in-process LLVM signal handler print
        # the symbolized crash stack; combined with the diag trace below it.
        "$EXE" --gtest_filter='SubDevices.BuildProgramForSubSubDevices' \
               --gtest_catch_exceptions=0 > build/adapter_uaf_console.log 2>&1
        echo "test exited rc=$?"
        echo "===== console (crash stack) =====" ; cat build/adapter_uaf_console.log || true
        echo "===== adapter identity trace =====" ; cat build/adapter_uaf_diag.log || echo "(no diag log)"
    - name: Upload adapter-UAF diag
      if: ${{ always() && contains(inputs.changes, 'sycl') }}
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
      with:
        name: adapter_uaf_diag
        path: |
          build/adapter_uaf_diag.log
          build/adapter_uaf_console.log
        if-no-files-found: warn
        retention-days: ${{ inputs.retention-days }}
    - name: check-llvm-spirv
      if: ${{ !cancelled() && contains(inputs.changes, 'llvm_spirv') }}
      run: |
        cmake --build build --target check-llvm-spirv
    - name: check-xptifw
      if: ${{ !cancelled() && contains(inputs.changes, 'xptifw') }}
      run: |
        cmake --build build --target check-xptifw
    - name: Generate/diff new ABI symbols
      if: ${{ !cancelled() && contains(inputs.changes, 'sycl') }}
      shell: bash
      run: |
        LLVM_BIN_PATH="build/bin" python.exe src/sycl/tools/abi_check.py --mode dump_symbols --output build/new_sycl_symbols_windows.dump build/bin/sycl?.dll
        diff -Naur src/sycl/test/abi/sycl_symbols_windows.dump build/new_sycl_symbols_windows.dump || true
    - name: Upload new ABI symbols
      if: ${{ !cancelled() && contains(inputs.changes, 'sycl') }}
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
      with:
        name: sycl_windows_abi_symbols
        path: build/new_sycl_symbols_windows.dump
        retention-days: ${{ inputs.retention-days }}
    - name: Install sycl-toolchain
      if: ${{ !cancelled() && steps.build.conclusion == 'success' }}
      run: |
        cmake --build build --target deploy-sycl-toolchain

    - name: Pack toolchain release
      if: ${{ !cancelled() && steps.build.conclusion == 'success' && inputs.release_toolchain_artifact != '' }}
      run: tar -czf ${{ inputs.toolchain_artifact_filename }} -C install .
    - name: Upload toolchain release
      if: ${{ !cancelled() && steps.build.conclusion == 'success' && inputs.release_toolchain_artifact != '' }}
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
      with:
        name: ${{ inputs.release_toolchain_artifact }}
        path: ${{ inputs.toolchain_artifact_filename }}
        retention-days: ${{ inputs.retention-days }}

    - name: Install utilities
      if: ${{ !cancelled() && steps.build.conclusion == 'success' }}
      shell: bash
      run: |
        cmake --build $GITHUB_WORKSPACE/build --target install-sycl-test-utilities

    - name: Pack toolchain
      if: ${{ !cancelled() && steps.build.conclusion == 'success' }}
      shell: bash
      run: |
        tar -czf ${{ inputs.toolchain_artifact_filename }} -C install .
    - name: Upload toolchain
      if: ${{ !cancelled() && steps.build.conclusion == 'success' }}
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
      with:
        name: sycl_windows_default
        path: ${{ inputs.toolchain_artifact_filename }}
        retention-days: ${{ inputs.retention-days }}

    - name: Setup SYCL toolchain
      if: ${{ !cancelled() && steps.build.conclusion == 'success' }}
      run: |
        echo "PATH=$env:GITHUB_WORKSPACE\\install\\bin;$env:PATH" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
        echo "LIB=$env:GITHUB_WORKSPACE\\install\\lib;$env:LIB" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

    - name: Build E2E tests
      if: ${{ inputs.e2e_binaries_artifact && !cancelled() && steps.build.conclusion == 'success' }}
      uses: ./devops/actions/run-tests/windows/e2e
      with:
        ref: ${{ inputs.ref || github.sha }}
        testing_mode: build-only
        target_devices: all
        binaries_artifact: ${{ inputs.e2e_binaries_artifact }}
        extra_lit_opts: --param sycl_build_targets="spir"
        cxx: ${{ inputs.cxx }}

    - name: Build E2E tests with New Offload Model
      if: |
        inputs.e2e_binaries_new_offload_model_artifact &&
        !cancelled() &&
        steps.build.conclusion == 'success' &&
        contains(github.event.pull_request.labels.*.name, 'new-offload-model')
      uses: ./devops/actions/run-tests/windows/e2e
      with:
        ref: ${{ inputs.ref || github.sha }}
        testing_mode: build-only
        target_devices: all
        binaries_artifact: ${{ inputs.e2e_binaries_new_offload_model_artifact }}
        extra_lit_opts: --param sycl_build_targets="spir" --param enable_new_offload_model=True
        cxx: ${{ inputs.cxx }}

    - name:  Detect hung tests
      if: always()
      shell: powershell
      run: |
        Invoke-WebRequest -Uri "https://raw.githubusercontent.com/intel/llvm/refs/heads/sycl/devops/scripts/windows_detect_hung_tests.ps1" -OutFile "windows_detect_hung_tests.ps1" -Headers @{Authorization = "Bearer ${{ github.token }}"}
        powershell.exe -File windows_detect_hung_tests.ps1
        $exitCode = $LASTEXITCODE
        Remove-Item -Path "windows_detect_hung_tests.ps1"
        exit $exitCode
