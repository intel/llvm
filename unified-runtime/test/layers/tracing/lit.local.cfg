import lit.formats

config.test_format = lit.formats.ShTest(True)
config.suffixes = [".match"]

# Always use the mock adapter
config.environment["UR_ADAPTERS_FORCE_LOAD"] = config.mock_adapter

config.substitutions.append((r"%xptienable", "UR_ENABLE_LAYERS=UR_LAYER_TRACING XPTI_TRACE_ENABLE=1 XPTI_FRAMEWORK_DISPATCHER={0}xptifw{1}".format(config.shlibpre, config.shlibext)))
